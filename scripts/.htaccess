# Safety net: if the repository root is ever exposed as a web root by mistake,
# block everything. The real entry point is public/index.php.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
